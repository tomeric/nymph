- if @gems.present?
  %h1 Loaded Gems
  %table.nymph-table
    %thead
      %tr
        %th.gem Gem
        %th.dependencies Dependencies
        %th.version Version
    %tbody
      = render :partial => "gem", :collection => @gems